# Release status

This branch packages BA Kit `1.0.0-rc.1` for acceptance review. It is not a public-release approval or a `BA KIT 1.0.0-rc.1 PASS` claim.

Before a public release, review every item marked `UNKNOWN` in [`PROVENANCE.md`](PROVENANCE.md), confirm redistribution rights and required notices, and run the fresh-session CR-001 acceptance in `kits/ba/acceptance.yaml`. Benchmark evidence includes a later 7/7 clean-session routing evaluation, while the earlier Stage 6C report still records its initial pending status; the package review must treat those as separate evidence records and evaluate this packaged version independently.
