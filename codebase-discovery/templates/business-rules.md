# <Concept> rules

> **Last updated:** YYYY-MM-DD
> **Scope:** Rules governing <concept> in <system>
> **Mode:** full | code-only
> **Status:** <full: accepted knowledge unless flagged | code-only: code-derived, not validated by a person> — see <../ | ../../>_discovery/assumptions-register.md

<!-- ONE RULE CLUSTER PER FILE. Written as `rules-<concept>.md`, inside areas/<area>/ when an area
owns it, or under domain/ when it applies system-wide. A second cluster is a SECOND FILE, not another
section here; that's how these docs stay small as the system grows.

NAMING: the cluster is only as good as its evidence. Until a stakeholder confirms it, name the file
after the code unit the rules already live in (rules-invoice-policy.md) and mark the grouping
[unverified] in the scope line above. A business-sounding cluster nobody agreed is invented
structure, and structure is stickier than prose. Once confirmed, rename to the glossary term
(rules-refund-eligibility.md) and drop the flag.

The rules a new joiner must know, NOT every conditional in the code. Keep citations in
the traceability index. -->

## Rules

- **<Rule name>** — <the rule, stated plainly>. Applies when <condition>. Exceptions:
  <who/what can override>. Rationale: <policy / regulation / technical, or [assumption] if
  the why is unknown>.

- **<Rule name>** — <...>

## Where they're enforced

<!-- Brief: the layer or component that enforces these, so a reader knows where a change would land.
Not a code listing; the traceability index holds the citations. -->

<!-- Use [assumption] where the rule is inferred from code but the intent/why isn't confirmed.
Use [outdated]/[contradicted] where an existing doc disagreed with the code. -->
